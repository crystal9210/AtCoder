#include <bits/stdc++.h>
using namespace std;
#define rep(i, m, x) for (int i = m; i < (x); i++)
#define rep_ll(i, m, x) for (ll i = m; i < (x); i++)
typedef long long ll;
typedef long double lb;

int main()
{

  ll k, sx, sy, tx, ty;
  cin >> k;
  cin >> sx >> sy >> tx >> ty;
  // ①x方向とy方向で大きなマスを何個通過するかを計算
  // ②調整として橋となる小さなマスを何個通過する必要があるかを計算する
  // 上のものから順にテストケースを満たすように実装を調整していく

  return 0;
}
