#include <bits/stdc++.h>
using namespace std;
#define rep(i,m,x) for(int i=m;i<(x);i++)
typedef long long ll;
// 問:on,offの状態を持つn個のスイッチとm個の電球がある。スイッチには1からnの電球には1からmの番号が付いている。電球iはki個のスイッチに繋がっており、スイッチsi1,si2,...,sikiのうちonになっているスイッチの個数を2で割った余りがpiに等しい時に点灯する。すべての電球が点灯するようなスイッチのon/offの状態の組み合わせは何通りあるか。

int main(){
  int n,m;
  cin>>n>>m;
  vector<int> is_on(n,false);

  return 0;
}
